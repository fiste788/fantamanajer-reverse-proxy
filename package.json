{
	"name": "fantamanajer-reverse-proxy",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy src/index.ts",
		"dev": "wrangler dev src/index.ts",
		"start-stackblitz": "WRANGLER_SEND_METRICS=false wrangler dev src/index.ts",
		"cf-typegen": "wrangler types",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^4.1.2",
		"wrangler": "^4.78.0"
	}
}
